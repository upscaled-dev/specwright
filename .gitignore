node_modules/
dist/
out/
coverage/

# Husky generated internals (not the hook scripts under .husky/<name>)
.husky/_/

# VS Code test scratch + extension dev artifacts
.vscode-test/
*.vsix
packages/

# Playwright outputs
playwright-report/
test-results/
.features-gen/
# The primary integration fixture ships a committed generated spec: exact spec-line resolution
# refuses to run without one, so the fixture must be real, not machine-local.
!src/test/integration/fixtures/workspace/.features-gen/
!src/test/integration/fixtures/workspace/.features-gen/**

# Integration-test scratch fixtures (created/torn down per run)
**/watcher-scratch/

# OS / editor noise
.DS_Store
*.log

.claude/
.agents/
.codex/
scripts/claude-sandbox.sh

# Internal working documents — not part of the shipped repo
docs/requirements/
AGENTS.md
CLAUDE.md
