# Paste your client id and secret

In the setup panel, paste the **Client ID** and **Client Secret** from the previous step.

Specwright runs a live **connection test** against your region host before saving, so you find out immediately if a value is wrong. Valid credentials are stored in the operating system's secret store, keyed by site, never in `settings.json` and never in git.
