# Run one scenario

Select one discovered scenario and run it. Specwright sends the same exact scenario intent through the active execution provider whether the run starts in Testing, the editor, or the command palette.

The first run needs a trusted, compatible workspace. Xray is not required.
