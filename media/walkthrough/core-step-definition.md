# Navigate or create a step definition

Place the cursor on a `Given`, `When`, or `Then` line in a feature file and choose **Go to Step Definition**.

If the step has no match, choose **Generate Missing Step Definitions** or use the editor quick fix. Specwright writes the definition under the configured step paths.
