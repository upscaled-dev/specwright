# Open Steps

The Steps panel groups definitions by keyword, shows usage counts, and lists unmatched Gherkin steps.

An empty panel links directly to **Diagnose workspace** and **Configure step paths**, so the next action stays visible.
